[project]
name = "reeds-finals-flashcards"
version = "1.0.0"
description = "Reed's Finals Flashcards — study UI for PKU spring 2026 finals decks"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
]
